interface:
  display_name: "Remote Operations"
  short_description: "Safely run SSH and SCP operations"
  default_prompt: "Use $running-remote-operations to run this remote operation while preserving local and remote boundaries."
